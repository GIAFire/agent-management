# AgentScope 智能体管理

AgentScope 是用于创建、配置和运行智能体的管理平台。本上下文统一项目中的核心领域术语，供需求、Issue、代码评审和测试命名共同使用。

## 身份与边界

**租户**：
平台内用于隔离用户、智能体及其相关资源的业务边界。
_避免使用_：组织、空间、项目

**用户**：
在租户范围内登录平台、管理资源或与智能体交互的主体。
_避免使用_：账号、租户

## 智能体

**智能体**：
平台中可被配置，并能与用户对话或执行目标的 AI 行为主体；当前不设草稿、启用或停用状态，创建成功后即可使用。当前统一采用 HARNESS 运行方式，不向用户开放运行类型选择。删除智能体时逻辑删除其身份和关联的智能体配置；能力绑定、会话与运行历史继续保留，如果它是本地子智能体的能力来源，则对应子智能体也一并逻辑删除。
_避免使用_：Agent、AI Agent、机器人

**智能体配置**：
与智能体一对一的当前生效结构化运行设置；现阶段不保留配置版本，编辑会直接更新唯一的原配置记录，并影响该智能体之后发起的运行。模型配置允许暂时为空，但未绑定可用模型配置时不能发起运行。
_避免使用_：智能体定义、智能体设置、Agent 版本

**智能体编码**：
智能体创建时确定、在租户内永久唯一且不可变的运行时标识；逻辑删除后也不释放。
_避免使用_：智能体名称、数据库主键

**子智能体**：
可由智能体委派工作的可复用能力定义，来源可以是平台内智能体或远程智能体服务；来源类型在创建后保持不变。本地智能体之间的委派关系不能形成直接或间接环路。
_避免使用_：子 Agent、从智能体、辅助智能体

**本地子智能体**：
以租户内某个智能体为能力来源的子智能体；它引用该智能体的当前配置，但拥有独立的子智能体编码、名称与描述。
_避免使用_：复制智能体、内置子 Agent

**远程子智能体**：
以远程 Agent Protocol 服务为能力来源的子智能体。
_避免使用_：外部接口、远程工具

## 对话与运行

**会话**：
用户与某个智能体之间的一段连续交互；会话关联该智能体的配置记录，配置被直接更新后，会话之后发起的运行使用更新后的配置。
_避免使用_：对话、聊天、线程

**会话状态**：
某个用户在一次会话中的完整可恢复运行快照，包括消息上下文、摘要、权限、计划、任务和工具状态。
_避免使用_：智能体状态、AgentState、记忆、聊天记录

**会话状态存储**：
负责保存和恢复会话状态的持久化能力，其存储位置由智能体配置在本地文件与 Redis 之间选择；切换存储类型时不迁移既有会话状态。
_避免使用_：StateStore、AgentStateStore、状态后端

**智能体工作区**：
由服务端配置的工作区根目录按租户、用户和智能体派生出的隔离文件目录；客户端不能指定或覆盖服务器文件系统路径。
_避免使用_：客户端工作目录、任意工作路径

**消息**：
会话时间线中的一个可展示条目，包括用户输入、智能体输出以及工具、技能、子智能体或计划产生的结果。
_避免使用_：事件、日志

**运行**：
会话中由一次用户请求触发的智能体执行过程；一次运行产生输出消息以及零个或多个运行事件。
_避免使用_：任务、调用、会话

**运行成功率**：
统计区间内成功运行数占已结束运行数的比例；成功、失败和取消属于已结束运行，运行中不进入分母。
_避免使用_：模型调用成功率、工具调用成功率

**运行事件**：
运行过程中产生的细粒度过程事实，例如文本增量、工具调用或权限请求。
_避免使用_：消息、运行日志

## 计划

**计划**：
智能体围绕用户目标形成的可审批、可执行工作方案，包含目标、风险、预期结果和生命周期状态。
_避免使用_：任务列表、执行记录

**计划任务**：
隶属于计划的结构化工作项，具有顺序、负责人、依赖关系、执行状态和完成证据。
_避免使用_：计划、普通任务

## 能力与资源

**模型配置**：
将模型供应商、接口协议、访问凭据、上游对话文本模型标识和运行参数组合后，平台注册并可供智能体配置引用的模型能力定义；它拥有租户内唯一的配置名称，同一个上游模型可以存在多套模型配置。
_避免使用_：模型、模型实例

**模型供应商**：
向租户提供模型服务的商业或部署主体，用于模型配置的展示、筛选与统计。
_避免使用_：接口协议、模型类型

**模型接口协议**：
模型服务对外提供的请求与响应规范，由运行时据此选择客户端适配方式。
_避免使用_：模型供应商、模型类型

**模型调用**：
智能体运行过程中，通过某个模型配置发起的一次逻辑模型生成请求；同一次运行可以产生多次模型调用，一次调用内的自动重试不单独计数。
_避免使用_：智能体运行、会话、调用事件

**系统提示词**：
可供智能体配置选择的租户内提示词模板；当前不设草稿、启用或停用状态，未删除记录均可选择。智能体只保存模板引用，模板内容的后续修改直接影响之后的运行；模板可在被引用时直接删除，失效引用按未配置系统提示词处理。
_避免使用_：智能体提示词副本、内嵌提示词

**工具**：
智能体为获取信息或改变外部状态而调用的明确能力。
_避免使用_：函数、插件、技能

**能力绑定**：
智能体配置对工具、技能、知识库或子智能体的当前选择关系；运行时只加载可用资源，编辑采用差异更新，并以逻辑删除保留被取消或已失效的绑定记录。
_避免使用_：能力副本、配置版本

**工具组**：
按用途或权限边界组织的一组工具。
_避免使用_：工具、工具集

**工具权限规则**：
在租户范围内，为某个工具与角色的组合规定允许、拒绝、询问或继续匹配后续规则的当前生效策略。
_避免使用_：权限变更记录、工具权限历史

**技能**：
由主说明和零个或多个资源文件组成、可被智能体按需加载的可复用能力。
_避免使用_：技能包、Skill、Agent Skill

**技能编码**：
技能创建时确定、在租户内唯一且不可变的运行时标识；新建技能时同时作为主说明元数据的初始名称。
_避免使用_：技能名称、来源、路由

**技能元数据**：
位于 `SKILL.md` 顶部 YAML 区块中的技能描述信息；其中 `name` 和 `description` 是技能保存后的名称与描述的权威来源。
_避免使用_：数据库元数据、标签、技能编码

**技能资源文件**：
隶属于某个技能、供其主说明引用的文本内容，例如参考资料、脚本、样例或其他资源。
_避免使用_：技能、工作区文件、附件

**技能使用**：
智能体加载某个技能、读取其资源或运行其脚本的一次可审计行为；仅查询可用技能目录不属于某个技能的使用。
_避免使用_：技能执行、工具调用

**技能角色范围**：
限定哪些用户角色可以在运行时使用某个技能的访问范围；“所有角色”对租户内任意角色开放。
_避免使用_：工具权限、技能绑定

**知识检索工具**：
智能体按需查询其配置所绑定知识库的统一工具。
_避免使用_：知识库、自动检索

**知识库**：
租户内供智能体检索的主题化知识集合，与具体检索后端相互独立。
_避免使用_：数据集、向量库、文档库

**知识源文件**：
知识文档在其生命周期内所引用的不可变原始内容载体，用于回源与重新处理。
_避免使用_：临时上传文件、工作区文件

**知识文档**：
纳入知识库、引用一个知识源文件并接受解析与检索处理的来源内容单元。
_避免使用_：工作区文件、知识切片

**知识切片**：
从知识文档中派生、可独立参与检索并保留来源关系的内容单元。
_避免使用_：文档、向量

**知识任务**：
对知识文档执行上传处理、删除或索引维护的一次可恢复后台工作；知识库删除不属于知识任务。
_避免使用_：计划任务、运行、临时线程
