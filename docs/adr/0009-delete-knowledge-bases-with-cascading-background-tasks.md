# 使用级联后台任务删除知识库

知识库可能同时持有智能体绑定、Milvus 向量、MySQL 知识切片、知识源文件和知识文档，无法在一个可靠的 HTTP 请求内原子清理，因此允许删除非空知识库并使用级联后台知识任务执行。只有启用状态的知识库可以提交删除；停用状态禁止上传、删除、提交任务、新绑定和检索，重新启用后恢复这些能力。提交删除后知识库立即进入 `DELETING`，禁止上传、检索、文档操作和新的智能体绑定；任务依次解除既有智能体绑定并清理全部向量、切片、源文件和文档。成功后保留知识库删除审计记录并释放租户内名称；失败时记录具体原因，不自动重试，用户可手动重新提交删除任务并保留历史。
