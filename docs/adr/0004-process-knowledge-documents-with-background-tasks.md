# 使用后台知识任务处理知识文档

**状态**：已被 ADR-0014 取代。

知识文档的解析、切片、Embedding 和索引写入通过可恢复的后台知识任务执行，以避免大文档和远程模型调用占用 HTTP 请求；上传只保存原始内容并登记知识文档，用户确认切片配置后再显式创建知识任务。提交接口立即返回任务 ID，前端查询任务状态与进度；失败任务不自动重试，用户排除问题后可手动重新提交为一条新任务。知识文档按 `UPLOADED → PARSING → CHUNKING → EMBEDDING → READY/FAILED` 记录处理状态。
