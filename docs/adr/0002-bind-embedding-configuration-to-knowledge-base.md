# 将 Embedding 配置绑定到知识库

每个知识库独立绑定一个由用户显式填写的 Embedding 端点、模型、API Key、向量维度和距离度量，以允许不同知识库按内容选择合适的向量表示；首版只支持 OpenAI Embeddings 兼容协议。API Key 只允许写入且不出现在查询响应、日志或任务快照中。创建知识库时先调用一次 Embedding API，并校验实际向量维度与配置一致；创建后允许轮换 API Key 或迁移模型 URL，但更新前必须再次验证连通性与维度。模型名、向量维度和距离度量不可直接修改，更换它们必须执行显式的全量索引重建。
