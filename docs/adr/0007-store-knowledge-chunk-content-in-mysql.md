# 在 MySQL 中保存知识切片全文

MySQL 保存知识切片全文及来源元数据，作为展示、引用、审计和索引核对的业务记录，Milvus 仅承担向量检索；该设计接受文本在 MySQL 与 Milvus 中重复存储的成本，以避免业务能力依赖向量库动态字段，并要求切片内容列使用足够容量的文本类型。检索命中还必须用 Milvus 元数据中的平台切片 ID 回查 MySQL，只返回当前存在且所属文档为 `READY` 的切片，从而隔离失败或过期任务可能留下的旧向量。
