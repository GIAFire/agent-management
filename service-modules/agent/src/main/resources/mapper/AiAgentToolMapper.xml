<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zw.agent.mapper.AiAgentToolMapper">

    <select id="agentBindTools" resultType="com.zw.agent.entity.DTO.AgentBindToolDTO" parameterType="Long">
        SELECT
            tic.*
        FROM
            ai_agent_tool atl
                LEFT JOIN ai_tool_info_config tic ON atl.tool_info_config_id = tic.id
        WHERE
            atl.agent_id = #{agentId}
        AND atl.tenant_id = #{tenantId}
    </select>
</mapper>
