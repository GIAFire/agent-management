<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zw.agent.mapper.AiToolRolePermissionMapper">

    <select id="getToolPermissionByUserId" resultType="map" parameterType="Long">
        SELECT DISTINCT
            trp.tool_name toolName
        FROM
            sys_user u
                LEFT JOIN sys_user_role ur ON u.id = ur.user_id
                LEFT JOIN sys_role r ON r.id = ur.role_id
                LEFT JOIN ai_tool_role_permission trp ON trp.role_id = r.id
        WHERE
            u.id = #{userId}
    </select>

    <select id="permissionListByRoleId" resultType="com.zw.agent.entity.AiToolRolePermissionEntity">
        SELECT
            *
        FROM ai_tool_role_permission
        <where>
            <if test="toolName != null and toolName != ''">
                AND tool_name = #{toolName}
            </if>
            <if test="roleCodes != null and roleCodes.size() > 0">
                AND role_code IN
                <foreach collection="roleCodes" item="roleCode" open="(" separator="," close=")">
                    #{roleCode}
                </foreach>
            </if>
            AND tenant_id = #{tenantId}
            AND status = 1
        </where>
    </select>
</mapper>
