## Agent skills

### Issue tracker

Issue 与 PRD 统一使用本仓库的 GitHub Issues 管理。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认 triage 标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单一上下文：领域文档位于根目录 `CONTEXT.md`，架构决策记录位于 `docs/adr/`。详见 `docs/agents/domain.md`。
